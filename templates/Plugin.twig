<?php
/**
 * @link        http://hiqdev.com/{{ config.package.name }}
 * @license     {{ config.package.license }}
 * @copyright   Copyright (c) {{ config.package.years}}, {{ config.vendor.titleAndHomepage }}
 */

namespace {{ config.package.namespace }};

class Plugin extends \hiqdev\pluginmanager\Plugin
{
    protected $_items = [
        'menus' => [
            [
                'class' => '{{ config.package.namespace }}\SidebarMenu',
            ],
        ],
        'modules' => [
            'client' => [
                'class' => '{{ config.package.namespace }}\Module',
            ],
        ],
    ];

}
