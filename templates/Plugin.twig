<?php

namespace {{ config.package.namespace }};

class Plugin extends \hiqdev\pluginmanager\Plugin
{
    protected $_items = [
        'menus' => [
            [
                'class' => '{{ config.package.namespace }}\SidebarMenu',
            ],
        ],
        'modules' => [
            'client' => [
                'class' => '{{ config.package.namespace }}\Module',
            ],
        ],
    ];
}
